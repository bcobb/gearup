#! /usr/bin/env ruby

require 'gearup'

args = ARGV.dup

worker = ::File.expand_path(args.pop)
configuration = Gearup::CommandLineConfiguration.from(args)

Gearup.configure(configuration)
Gearup.run_from_file(worker)
