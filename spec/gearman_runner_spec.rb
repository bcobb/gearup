require 'gearup'

describe Gearup::GearmanRunner do

  it "reports the worker's ability to Gearman"

  it "can set a timeout for the worker's ability"

  it "can run a block after the worker's ability"

  it "can configure the worker's sleep interval time"

  it "can configure the worker's maximum time to read data from the server"

  it "reports if the worker has seen any bad servers"

  it "reports if the worker is enabled"

  it "reports the worker's status"

end
